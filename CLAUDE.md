# dadaschool.github.io

교사가 직접 운영하는 GitHub Pages 정적 사이트. 빌드 도구 없음, HTML 파일을 그대로 서빙한다.

`geoje/` 작업을 이어받는 경우 **[`HANDOFF.md`](./HANDOFF.md)를 먼저 읽을 것.** 어떤 영상을 왜 채택했고 무엇을 왜 배제했는지, 이미 끝난 판단이 정리되어 있다.

## 공개 규칙 (가장 중요)

1. **`geoje/` 아래에 만드는 것은 루트 사이트에 노출하지 않는다.**
   루트(`/index.html`)의 목록, 네비게이션, 카드, 사이트맵 어디에도 링크를 추가하지 말 것.
   주소를 직접 아는 사람만 접근하는 것이 의도된 동작이다.

2. **루트에 노출할지 여부는 임의로 판단하지 않는다.**
   노출이 필요해 보이더라도 먼저 사용자에게 묻고, 명시적인 답을 받은 뒤에만 반영한다.
   "당연히 링크가 있어야 편할 것 같아서" 추가하는 것은 이 규칙 위반이다.

3. 새 하위 디렉터리를 만들 때도 위 원칙을 기본값으로 삼는다.
   루트 노출은 옵트인이며, 기본은 비노출이다.

4. 🚨 **`binary/` 는 루트 목록에서 빼 두었다** (2026-08-24 사용자 지시).
   폴더와 주소(`https://dadaschool.github.io/binary/`)는 **그대로 살아 있다** — 목록에만 없다.
   *왜* : 그 앱(10진수 → 2진수 변환 학습기)이 하던 일을 `data-convert/`
   (아날로그 → 디지털 변환 학습기)가 더 넓게 덮는다. 두 줄이 나란히 있으면
   학생·교사가 어느 것을 열어야 하는지 알 수 없다.
   ⚠ 이 저장소 밖의 규칙(프로젝트 루트 `CLAUDE.md` 의 「폴더에 `index.html` 이 있으면
   루트 목록에 한 줄 넣는다」)을 그대로 따르면 **다음 세션이 이 줄을 되살린다.**
   `geoje/` 와 같이 **일부러 뺀 것**이므로 되살리기 전에 반드시 물을 것.

## 구조

```
/index.html          루트 사이트 — 위 규칙에 따라 함부로 수정하지 않는다
/binary/             10진수 → 2진수 변환 학습기 (주소는 살아 있고 루트 목록에서만 뺐다)
/geoje/index.html    중학교 학교폭력 예방 수업 영상 자료 (비노출)
/geoje/README.md     디렉터리 단위 비노출 규칙 안내 (웹 비발행)
/_config.yml         Jekyll 제외 목록 — 아래 문서들을 웹에 발행하지 않는다
/CLAUDE.md           이 파일 (웹 비발행)
/HANDOFF.md          geoje 작업 인계 문서 — 선정 근거와 남은 작업 (웹 비발행)
```

## 페이지 작성 규칙

- **단일 파일 HTML.** CSS와 JS는 같은 파일 안에 인라인으로 둔다. 번들러나 프레임워크를 도입하지 않는다.
- 폰트는 CDN 링크로 불러온다 (Google Fonts, jsDelivr).
- `lang="ko"`, 반응형(모바일 우선), 키보드 포커스 표시, `prefers-reduced-motion` 존중은 기본으로 지킨다.
- 한국어 본문에는 `word-break: keep-all`을 적용한다.
- 비노출 페이지에는 `<meta name="robots" content="noindex, nofollow">`를 넣는다.

## 교육자료 페이지를 다룰 때

이 사이트의 페이지는 수업에서 실제로 학생에게 보여주는 자료를 안내한다. 잘못된 정보는 교실에서 바로 사고가 된다.

- **외부 영상 링크는 반드시 실제로 확인한 것만 넣는다.** 기억에 의존해 URL이나 제목을 지어내지 말 것. 재생시간, 제작처, 업로드 채널을 확인하고 페이지에 표기한다.
- 재생시간을 표기했다면 그 값이 실제와 맞아야 한다. 영상이 교체되거나 비공개 전환될 수 있으니 수정 작업 시 링크 생존 여부를 함께 점검한다.
- 대상 학년을 명시한다. 현재 `geoje/`는 **중학생 대상**이며 초등 수준 자료는 넣지 않는다.
- 학교폭력 소재 자료는 가해 수법을 생생히 재연하는 영상, 욕설이 많은 영상, "참교육·복수" 계열 오락 콘텐츠를 배제한다. 교육청·경찰청·푸른나무재단 등 공공기관 제작물을 우선한다.
- 상영 전 교사가 직접 확인해야 한다는 안내와 신고 창구(117)는 페이지에서 빼지 않는다.

## 배포

`main` 브랜치에 푸시하면 GitHub Pages가 1~2분 내 반영한다. 별도 빌드 단계 없음.
