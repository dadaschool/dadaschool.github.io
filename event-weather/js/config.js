/**
 * config.js — 예보 중계가 어디에 있는지
 *
 * ─────────────────────────────────────────────────────────────
 * 🔴 비워 두어도 앱은 그대로 돈다.
 *    비어 있으면 화면이 «같은 자리의 서버»(교사 PC 의 server.py)에게 묻고,
 *    그것도 없으면 «이 사이트에서는 과거 통계만 봅니다» 라고 말한다.
 *    과거 통계(모드 A)는 중계가 없어도 언제나 된다.
 *
 * 🔒 여기에 인증키를 적지 말 것. **주소만** 적는다.
 *    키는 Cloudflare 의 Secret 에만 있고 브라우저로 내려오지 않는다.
 *    (배포 스크립트가 키처럼 보이는 글자를 보면 멈춘다)
 *
 * 만드는 방법은 `worker/설치안내.md` 에 있다 (약 10분).
 * 만든 뒤 아래 한 줄만 고치면 사이트에서도 예보가 보인다 :
 *
 *     window.예보중계 = "https://event-weather.<내계정>.workers.dev";
 *
 * ⚠ 끝에 «/» 를 붙이지 않는다.
 * ─────────────────────────────────────────────────────────────
 */

window.예보중계 = "https://event-weather.edudadat.workers.dev";
