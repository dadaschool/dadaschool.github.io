/* =========================================================
   config.js — 문제를 어디에 담을지 정하는 **한 줄**

   `WORKER` 를 채우면 그때부터 **사이트만으로** 문제를 만들고 고치고 지운다.
   교사 PC 에서 `python server.py` 를 켤 필요가 없다(2026-08-24 사용자 지시).

   만드는 방법은 `worker/설치안내.md` — 한 번만 하면 된다(약 10분).
   만든 뒤 아래 주소를 붙여넣고 `python tools/사이트에_올리기.py --push` 를 돌린다.

   ⚠ 주소 끝에 `/` 를 붙이지 말 것.
   ⚠ 이 파일에 **교사 코드를 적지 말 것.** 코드는 Cloudflare 의 변수(`TEACHER_HASH`)와
     `js/locked.js` 의 암호문에만 있다. 이 파일은 사이트에 그대로 공개된다.

   비워 두면 예전처럼 움직인다 :
     ① 교사 PC 의 server.py  ② 사이트에 함께 올려 둔 `q/NNNNNN.json` 파일
   ========================================================= */
window.CONFIG = {
  WORKER: ""      // 예 : "https://mbconnect.edudadat.workers.dev"
};
